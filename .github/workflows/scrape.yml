name: Weekly Deal Scrape

on:
  schedule:
    # Runs every Monday at 8am UTC (3am EST) to catch new weekly circulars
    - cron: "0 8 * * 1"
  workflow_dispatch: # Allow manual trigger

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run scraper
        run: node scripts/scrape-deals.js

      - name: Commit updated deals
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/deals.json
          git diff --staged --quiet || git commit -m "Update weekly deals [automated]"
          git push
